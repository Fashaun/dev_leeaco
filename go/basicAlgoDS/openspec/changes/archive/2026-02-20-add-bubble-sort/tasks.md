## 1. Bubble Sort Package

- [x] 1.1 Create `bubble_sort/` directory
- [x] 1.2 Create `bubble_sort/bubble_sort.go` with `BubbleSort(arr []int)` function that sorts in-place using bubble sort algorithm
- [x] 1.3 Add `Run()` function that prints an example array before and after sorting

## 2. CLI Registration

- [x] 2.1 Add `"basicAlgoDS/bubble_sort"` import to `main.go`
- [x] 2.2 Add `case "bubble_sort"` to switch in `main.go` calling `bubble_sort.Run()`

## 3. Verification

- [x] 3.1 Run `go run main.go bubble_sort` and verify sorting demo output
- [x] 3.2 Run `go run main.go unknown` and verify error message still works
