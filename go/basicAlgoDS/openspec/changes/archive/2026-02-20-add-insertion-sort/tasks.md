## 1. Create insertion_sort package

- [x] 1.1 Create `insertion_sort/` directory and `insertion_sort/insertion_sort.go` with package declaration
- [x] 1.2 Implement `InsertionSort(arr []int)` function that sorts in-place in ascending order using the shifting approach

## 2. Run demo function

- [x] 2.1 Implement `Run()` function that prints array before and after sorting

## 3. Register in CLI dispatcher

- [x] 3.1 Add `insertion_sort` import and switch case in `main.go`

## 4. Verify

- [x] 4.1 Run `go run main.go insertion_sort` and verify output
